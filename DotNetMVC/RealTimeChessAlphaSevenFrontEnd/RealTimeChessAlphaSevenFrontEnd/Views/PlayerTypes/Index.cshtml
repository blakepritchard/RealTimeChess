@model IEnumerable<RealTimeChessAlphaSevenFrontEnd.Models.PlayerType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PlayerTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TurnOrder)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PlayerTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TurnOrder)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PlayerTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PlayerTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlayerTypeId })
        </td>
    </tr>
}

</table>
